spring:
  application:
    name: gatewayServer

eureka:
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://eureka-service.eastus.azurecontainer.io:8761/eureka # TODO